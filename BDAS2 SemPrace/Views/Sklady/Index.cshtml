@model IEnumerable<BDAS2_SemPrace.Models.Sklady>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Nazev)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.IdAdresaNavigation)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Nazev)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.IdAdresaNavigation.Mesto)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.IdSklad">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.IdSklad">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.IdSklad">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
